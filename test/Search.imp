begin
    s = [45, 68, 23] ;
    bi = 0 ;
    len = 3 ;
    bs = len ;
    m = (bi+bs)/2 ;
    while bi < bs and x != s[m] do
        m = (bi+bs)/2 ;
        if s[m] < x then
            bi = m+1 ;
        else
            bs = m
        endif
    done
    if len <= m or s[m] != x then
        m = -1 ;
    endif
    print(m)
end
